/**
 * GestureHint - legacy, kept for compatibility
 * Gesture hints are now inline in CoachSession
 */

'use client';

import React from 'react';

export default function GestureHint() {
  return null;
}
